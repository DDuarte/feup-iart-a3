namespace LandAllocationsLib.StateRepresentation
{
    /// <summary>
    /// "Something" on the map that isn't a lot or landuse
    /// </summary>
    public enum Place
    {
        Lake,
        Highway
    }
}
